import { sumar, restar } from './funciones.js';

//puedes renombrar funciones para no colisionar los nombres de ellas
// se hace con as ejemplo= import{ sumas as funcionSumar}

const resultado  = sumar(20, 30)
const resultado1  = restar(20, 30)

console.log(resultado);
console.log(resultado1);